<nav class="navbar navbar-inverse navbar-static-top">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu" aria-expanded=" false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'To-do List', root_url, class: 'navbar-brand' %>
    </div>

  <!-- Conteudo exibido em telas maiores e que serao escondidos em telas menores -->
  <div class="collapse navbar-collapse" id="expanded-menu">
    <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to 'Tasks', '#' %>
        </li>
    </ul>

    <ul class="nav navbar-nav navbar-right">
     <% if user_signed_in?%>
        <li class="active">
          <%= link_to current_user.name.capitalize, '#' %>
        </li>
        <li class="active">
          <%= link_to 'Sair', destroy_user_session_path, method: :delete %>
        </li>
      <% else %>
        <li class="active">
          <%= link_to 'Nova Conta', new_registration_path(resource_name) %>
        </li>
        <li>
          <%= link_to 'Entrar', new_session_path(resource_name) %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>